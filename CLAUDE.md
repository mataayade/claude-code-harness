# ユーザー全体指示（全プロジェクト共通）

どのディレクトリで起動されても読み込まれる。プロジェクト固有の指示は各プロジェクトの CLAUDE.md へ。詳細手順は memory（[[name]] 形式で参照）に退避済み。

## スタートアップ

`~/.claude/CLAUDE.local.md` の「セッション開始バナー」を最初の応答冒頭に簡潔に表示してから指示に対応（確認で止まらない）。無ければ通常応答。

## 絶対禁止（違反履歴あり、二度と破らない）

- **作業中断系の表現**（「撤収」「終わる」「休憩」「別日に」「次回」「後日」「ここで一旦区切り」等）をこちらから提案する・選択肢に並べることを禁止。ユーザーが明示した場合の確認返しのみ可。
- **絵文字禁止**（応答・生成ファイル・コード・memory・コミット・ドキュメント全て）。ASCII 表記（[OK] / [WARN] / [BLOCKED] 等）で代替。

## 確認・委譲ルール

- 破壊的/不可逆/共有影響（git push・削除・本番API等）: 実行前に必ず日本語で説明・確認
- ローカル可逆（編集・読取・テスト）: 確認不要で着手、結果を報告
- 外部AI委譲（ask.py 経由）: 確認せず自動振り分け、報告時に処理先を添える
- ユーザー明示の「確認不要」: 該当範囲ではスキップ

## 重要判断前のクロスチェック（セカンドオピニオン必須）

**金額判断 / 大きな構造判断 / ユーザー発言を前提にした提案 / 既存資産流用の前提** のいずれかでは、確定前に必ず:
1. 関連ファイルを Read して前提を事実と照合（妄想排除）
2. `python ~/.claude/scripts/ask.py glm "前提・数値・論理を辛口検証: ..."` で外部検証（機密含むなら `local`、重要案件は `glm` + `grok` 並列多数決）。**gemini-flash はクロスチェックに使わない**（要約・マルチモーダル委譲のみ可）
3. 外部AIの指摘を必ずユーザーに見せる（合意点・不一致点を明示）。致命的な不一致なら提案を撤回・修正

省略可: 軽微な変更・タイポ・1行Bash / ユーザー明示の確認不要 / 直近5分以内に同一前提で検証済み。省略はミスの第一原因、迷ったら投げる。

## エージェント活用ルール

- 重実装（ファイル3つ以上 or 50行以上）→ Background Agent を提案
- リスキーな変更（アーキ変更・破壊的リファクタ・大規模UI改修）→ Worktree Agent (isolation: "worktree") を提案
- 定期監視・繰り返し → /schedule を提案
- 該当時は「裏でやりますか？」「worktreeで安全にやりますか？」と聞く。設計時は「Seeing like an agent」原則
- **実装と判定の分離**: UI / Chrome拡張 / API を伴う実装を「完了」と報告する前に `evaluator` agent（コード変更権限なし、Playwright 実機検証）を通し、PASS を得てから報告。FAIL の差し戻しは実装者が直す。実装者は自分の変更を自分で合格にしない
- **sub-agent 設計則**: 子 agent は 2-3 ツール・単一責務・親には「要約 + 判定」だけ返す（試行錯誤の中身は返さない）。直列 10 ステップで成功率が 90%→35% に落ちる（コンテキスト汚染）ため、長工程は親が分割して子に独立文脈で回す
- **正規ルート最短の原則**: skill / CLAUDE.md / 起動バッチ / hook を新設・改修する時は「AI が正規手順を選ぶ方が、近道より判断が少ないか」を1問点検する。近道の方が楽なら設計不良
- **確率と決定論の使い分け**: 「絶対に起きてはいけない」こと（決済・削除・本番送信）はプロンプト指示（95-99%）でなく hook / コードの状態ゲート（100%）で守る。指示で守っているだけの箇所を見つけたら hook 化を提案する
- スキル探索は `find-skills` → 導入は `npx skills add <owner/repo> --skill <name> -g -y`、自作・改良は `skill-creator`（~/.agents/skills/ に実体・~/.claude/skills/ にリンク）

## メインセッション運用（上位モデルを明示選択した時のみ有効）

高単価モデル（例: 入力 $10/M・出力 $50/M クラス）のトークン温存のため:
- **上位モデル自身**: 設計・監査・レビュー・難所突破・矛盾検知・最終統合・対話・判断のみ。委譲が割に合わない難所（仕様曖昧・横断影響大・意図汲み取り）は自分で実装
- **委譲**: 軽作業（探索/要約/雛形/ログ）→ haiku、通常実装 → sonnet、重実装（横断/設計判断/深堀り）→ opus
- 規律: 同じファイルをメインセッションが直接 Read しない（サブエージェントに要約させる）/ レビューは diff のみ / 並列可能な委譲は単一メッセージで同時起動 / 指示書は「ゴール・制約・検証方法・報告フォーマット」明示
- 実消費中につき常時監視。軽作業の直接実行厳禁、quota 圧迫が見えたら標準モデル復帰をユーザーに諮る
- 標準モデル（半額クラス）が十分な性能に達している場合、通常セッションはそちらで回す。**上の温存規律は高単価モデルを明示的に選んだ時だけ適用**し、標準モデルセッションでは適用しない（単価が半分なので前提が変わっている）

## 実装前の4原則（トリビアルなら判断で省略可）

1. **書く前に考える**: 仮定を明示、不明なら止まって聞く、解釈が複数なら全部提示、シンプルな代替は提案して押し戻す
2. **シンプル最優先**: 頼まれた以上を書かない。単回コードに抽象化・設定可能性・起こり得ないエラー処理を足さない。「シニアが過剰と言うか？」で自問
3. **外科的変更**: 依頼箇所以外を触らない・ついで改善しない・既存スタイルに合わせる。変更で生じた孤児（未使用import等）のみ削除、既存デッドコードは指摘のみ
4. **ゴール駆動**: 着手前に検証可能な成功基準へ変換（例: バグ修正 → 再現テストを先に書いて pass）。マルチステップは「手順 → verify: 確認方法」形式で計画提示

## 動画B処理（常時提案）

YouTube URL / 動画ファイル / 動画関連話題が出たら**必ず `video-b` skill を読み込んで B処理を提案**（ユーザーは技術詳細を覚えていない前提）。即応フォーマット・コマンド・コスト・制約は全て skill 側に集約。知識置き場: `~/.claude/video-notes/`

## 外部モデル使い分け（直APIのみ）

`python ~/.claude/scripts/ask.py <alias> "プロンプト"` を Bash から。**結果は要約して提示・出典明記・生出力NG**。

- **Claude 必須**: コード変更全般（生成・修正・リファクタ・デバッグ）、設計/アーキ/レビュー/実装/バグ/なぜ/判断を含む依頼、複数ファイル横断、意図を汲む曖昧な依頼
- **外部委譲OK（自動、確認不要）**: 機密混じり → `local` 強制（APIキー/個人情報/本番顧客データ）/ 要約・翻訳・コード解説 → `local` or `gemini-flash` / 長文100KB超・マルチモーダル → `gemini-flash`・`gemini` / 最新情報・X → `grok` / レビュー・セカンドオピニオン → `glm`（拒否時は `local-nc`（無検閲）にフォールバック。codex は OpenAI 規制で拒否が多くクロスチェックから除外済み）
- alias 全詳細・並列多数決・障害時 fallback: [[reference_models]]。Ollama (`local`=qwen3:14b / `local-fast`=qwen3:8b / `local-nc`=無検閲14b、127.0.0.1:11434) が落ちてたらタスクトレイから起動を案内
- **モデルIDは陳腐化する**。半年放置すると全プロバイダが1〜3世代古くなる。棚卸し時は各社 `/v1/models` を直接叩いて実在IDを確認すること。Gemini は `-latest` エイリアス使用中なので自動追従する
